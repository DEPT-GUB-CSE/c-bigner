#include <stdio.h>
#include <stdlib.h>

int main()
{
    int h, w, area;
    printf("Input the value of hight and wight-\n");
    scanf("%d %d",&h, &w);
    area=0.5*h*w;
    printf("Area=%d",area);
}
