#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("blow up");
    int i, sum;
    scanf("%d", &sum);
    for(i=0;i<5;i++)
    {
        sum =sum+i;
            printf("sum= %d",sum);
}
    return 0;
}
